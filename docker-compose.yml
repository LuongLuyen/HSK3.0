name: hsk3.0

services:
  app:
    build: .
    image: hsk3.0
    container_name: hsk3.0
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_PATH: /app/data/app.db
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
